// HyperStream C API implementation stub (Increment 1 scaffolding)
// This TU will provide the monomorphized dispatch and wrappers.
// For initial scaffolding, we only include the header and prepare for export.

#define HYPERSTREAM_C_API_BUILD 1

#include "hyperstream/hyperstream.h"

// Implementation will follow in subsequent commits.

extern "C" HYPERSTREAM_C_API_EXPORT int hs_c_api_link_anchor(void) {
  return 0;
}

// Intentionally empty to allow building shared library and linking C test harness.
